<resources>

    <!-- AppTheme: applied after React Native finishes loading -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>

    <!-- SplashTheme: applied to MainActivity immediately on launch.
         Shows the brand-blue background while the JS bundle loads.
         MainActivity switches to AppTheme before React renders. -->
    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:statusBarColor">#2563EB</item>
        <item name="android:navigationBarColor">#2563EB</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

</resources>
